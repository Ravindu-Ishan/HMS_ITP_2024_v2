import React from "react";

function EmptyNavArea() {
  //use this component in pages where you want the nav area to be empty, in pages where a top nav bar does not exist

  return <div className="navarea"></div>;
}

export default EmptyNavArea;
